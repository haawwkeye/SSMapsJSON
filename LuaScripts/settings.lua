local settings = {
    CovertMap = {

    };
    FakeCursor = {
        Color = {89, 89, 255}; -- Cursor Color
        offset = 0; -- seconds offset 1 would be 1 second off
        ShowInside = true; -- Should it show the inside of the cursor
        RandomPos = true; -- Should it attempt to go to a random pos inside the note
         -- AnimateToNext = true; -- Should it animate while moving to the next note (TEMP REMOVED)
    };
}

return settings